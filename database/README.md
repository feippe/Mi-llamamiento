# Base de datos

Importa primero `database/schema.sql` y luego `seed-catalogo.sql`.

```sql
SOURCE database/schema.sql;
SOURCE seed-catalogo.sql;
```

El esquema incluye una `Estaca Demo` y un `Barrio Demo` para permitir el login de desarrollo sin configurar catálogos previos.
